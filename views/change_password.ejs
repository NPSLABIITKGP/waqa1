<!-- <link rel="stylesheet" href="styles/change_password.style.css">
<main>
    <div class="change-password-form">
        <div class="card-header">Change Password</div>
        <% if (errors.length > 0) { %>
            <ul>
                <% errors.forEach(error => { %>
                    <li><%= error.msg %></li>
                <% }) %>
            </ul>
        <% } %>
        <form action="/change_password" method="POST">
            <label>Current Password</label>
            <input type="password" name="current_password" required>
            <label>New Password</label>
            <input type="password" name="new_password" required>
            <label>Confirm New Password</label>
            <input type="password" name="confirm_password" required>
            <button class="btn btn-primary" type="submit">Change Password</button>
        </form>
    </div>
</main> -->


<link rel="stylesheet" href="/styles/change_password.style.css">

<main class="main-change-password">
    <div class="change-password-container">
        <div class="change-password-form">
            <h2 class="change-password-heading">Change Password</h2>

            <% if (errors.length > 0) { %>
                <div class="alert alert-danger">
                    <ul>
                        <% errors.forEach(error => { %>
                            <li><%= error.msg %></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>

            <form action="/change_password" method="POST">
                <div class="form-group">
                    <label for="current_password">Current Password</label>
                    <input type="password" id="current_password" name="current_password" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="new_password">New Password</label>
                    <input type="password" id="new_password" name="new_password" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="confirm_password">Confirm New Password</label>
                    <input type="password" id="confirm_password" name="confirm_password" class="form-control" required>
                </div>
                <button class="btn btn-change-password" type="submit">Change Password</button>
            </form>
        </div>
        <div class="password-info">
            <div class="password-info-content">
                <h3 class="password-help">Need Help?</h3>
                <p class="help-text">If you are facing issues, please reach out to our support team for assistance!</p>
                <button class="btn btn-help" onclick="window.location.href='/faq'">FAQ</button>
                <p class="help-text">If you're not in the mood to change it right now, that's cool too. 😎  
                    Why not head back and keep exploring?</p>
                <button class="btn btn-help" onclick="window.location.href='/welcome'">GO...</button>
            </div>
        </div>
    </div>
</main>
